# H2 Database Configuration
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:dailytrackerdb
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# JPA Configuration
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
# Use create-drop if you want the database to be created and dropped on each application start/stop
# spring.jpa.hibernate.ddl-auto=create-drop

# Show SQL statements (optional, good for debugging)
spring.jpa.show-sql=true

# Server port (optional, defaults to 8080)
# server.port=8081

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/dailytrackerdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password= # Please set your MySQL password here
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update # This is duplicated, removing one

# Redis Configuration
spring.redis.host=localhost
spring.redis.port=6379
# spring.redis.password= # Uncomment and set if your Redis has a password
# spring.cache.type=redis # To enable Spring Cache with Redis
